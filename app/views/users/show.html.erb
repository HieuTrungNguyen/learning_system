<% provide :title, @user.name %>

<% if logged_in? %>
  <div class="row">
    <aside class="col-md-2 info">
      <section class="user_info">
        <%= render "shared/user_info" %>
      </section>
      <section class="stats">
        <%= render "shared/stats" %>
      </section>
      <section class="button">
        <%= render "follow_form" %>
      </section>
    </aside>
    <aside class="col-md-6 activity">
      <section>
        <h1><%= t ".activities" %></h1>
        <% if @activities.any? %>
          <ul>
            <%= render @activities %>
          </ul>
          <%= link_to t(".more"), activities_path(@user) %>
        <% end %>
      </section>
    </aside>
    <aside class="col-md-4 result">
      <h2 class="show_result"><%= t ".show_result" %></h2>
      <%= render @lessons %>
    </aside>
  </div>
<% end %>
